version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: sigints
      POSTGRES_USER: sigints
      POSTGRES_PASSWORD: sigints
    ports:
      - "5432:5432"
    volumes:
      - sigints-postgres:/var/lib/postgresql/data

volumes:
  sigints-postgres:
